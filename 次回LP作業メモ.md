# 次回 LP ブラッシュアップ用メモ（Cursor 会話の引き継ぎ）

## 2026年5月14日に確認したこと

- **キャンペーン表記の締切**: 5月17日（日）まで。`index.html` 内の文言・メタタグはすべて「5/17(日)まで」で統一されている。
- **5月16日・17日（最終日前後）の表示**: 締切日で自動非表示にするコードはない。料金の日割り計算・残り人数表示もその日付で問題なく動く想定。
- **5月18日以降**: 文言は「5/17まで」のまま残る（自動で切り替わらない）。キャンペーン終了後は手動で差し替えが必要。

## 次回やるかもしれないこと

- **5月18日以降**: 新しいキャンペーン案をこちら（四月のエルピー側）で出し、`index.html` を差し替え・ブラッシュアップする可能性あり。
- 次回 Cursor で相談するときは、このファイルと `index.html` を開いた状態で、「前回メモの続き」「新キャンペーンの条件」を書いてもらえるとスムーズ。

## 技術メモ（開発向け・要約）

- 日割り・残り人数は `index.html` 末尾の `<script>` 内。年は `2026` 前提の箇所あり。新キャンペーン時は日付・金額・文言をまとめて確認すること。
- OGP の `REPLACE_ORIGIN` は公開 URL 決まり次第の置換が必要（コメント参照）。
