# 次回 LP ブラッシュアップ用メモ（Cursor 会話の引き継ぎ）

## 2026年5月14日に確認したこと

- 当初は 5/17 締切・先着10名だったが、後日更新あり（下記参照）。

## 2026年5月（更新）

- **キャンペーン名**: GW表記を廃止し「**5月限定キャンペーン**」に統一。
- **残り人数**: 5/17〜5/31で20名→1名（最終日1名）。表示は上部バナーのみ。
- **キャンペーン表記の締切**: **5月31日（日）まで**。`index.html` 内の文言・メタタグは「5/31(日)まで」で統一。
- **先着人数**: **20名様**（画面上部バナー・残り人数の自動カウントも初期値20）。
- **デザイン**: 紙吹雪・キラキラ・バウンスを廃止。落ち着いた背景＋価格の縁取りでお得感を訴求。
- **5月31日（最終日）の表示**: 締切日で自動非表示にするコードはない。最終日前後もページはそのまま表示される。
- **6月1日以降**: 文言は「5/31まで」のまま残る（自動で切り替わらない）。終了後は手動で差し替えが必要。

## 次回やるかもしれないこと

- キャンペーン終了後、新しい案をこちら（四月のエルピー側）で出し、`index.html` を差し替え・ブラッシュアップする可能性あり。
- 次回 Cursor で相談するときは、このファイルと `index.html` を開いた状態で依頼するとスムーズ。

## 技術メモ（開発向け・要約）

- 日割り・残り人数は `index.html` 末尾の `<script>` 内。年は `2026` 前提の箇所あり。新キャンペーン時は日付・金額・文言をまとめて確認すること。
- 残り人数: 5/6起点、1日1名減、上限20・下限1。
- OGP の `REPLACE_ORIGIN` は公開 URL 決まり次第の置換が必要（コメント参照）。
